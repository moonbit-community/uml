@startuml
start
:Read next job;

if (Job available?) then (yes)
  label execute
  :Execute job;
  if (Another job?) then (yes)
    goto execute
  else (no)
    :Finish queue;
  endif
else (no)
  :Wait for work;
endif

stop
@enduml
